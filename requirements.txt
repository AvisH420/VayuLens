# Core runtime (required)
fastapi
pydantic
uvicorn
# optional / recommended (install per environment)
sentence-transformers  # for transformer embeddings (heavy)
faiss-cpu             # optional, for vector index (or use memory backend)
chromadb              # optional, chroma vector store
python-docx           # .docx parser
pypdf                 # PDF text extraction
pytesseract           # OCR (optional)
pdf2image             # OCR helper (optional)
rank_bm25             # optional BM25 (fallback available)
# dev / testing
pytest
