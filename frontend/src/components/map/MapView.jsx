// Map + layers (grid cells, pollutant heatmap, attribution overlays).
// TODO: implement. Stub only.
export default function MapView() {
  return <section className="map-view">{/* map + layers go here */}</section>;
}
